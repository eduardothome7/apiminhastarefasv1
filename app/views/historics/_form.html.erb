<%= form_with(model: historic, local: true) do |form| %>
  <% if historic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(historic.errors.count, "error") %> prohibited this historic from being saved:</h2>

      <ul>
      <% historic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.number_field :task_id, id: :historic_task_id %>
  </div>

  <div class="field">
    <%= form.label :play %>
    <%= form.text_field :play, id: :historic_play %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
